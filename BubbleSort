Class BubbleSort
{
  int[] arr = {23,12,11,2,54,6,88,34,2};
  int temp =0;
  for(int i=0;i<arr.Length-1;i++)
  {
    for(int j=0;j<arr.Length-i-1;j++)
    {
      if(arr[j]>arr[j+1])
      {
        temp = arr[j];
        arr[j] = arr[j+1];
        arr[j+1] = temp;
      }
    }
  }
}
